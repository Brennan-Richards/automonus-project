import React, { Component } from "react";
import { render } from "react-dom";

class Navbar extends Component {

  render() {
    return(
      <nav className="navbar">
        <h1 style="float:left;">The Automonus Project</h1>
      </nav>
    );
  }
}

export default Navbar;
